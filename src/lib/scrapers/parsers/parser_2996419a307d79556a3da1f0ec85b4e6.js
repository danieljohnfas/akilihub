// The provided HTML does not contain actual job postings.
// It appears to be a general information page for UNICEF Tanzania, not a job board.
// According to CRITICAL INSTRUCTION 2, if the HTML does NOT contain real job postings,
// the result array must be left empty. No companies should be extracted as jobs.
// Therefore, no jobs will be extracted from this specific HTML.