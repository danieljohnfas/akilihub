// The provided HTML does not contain any job postings.
// It is a country profile page for CARE International in Tanzania,
// providing general information about their work in the region,
// rather than a list of job vacancies.
//
// As per CRITICAL INSTRUCTION #2:
// "If this HTML does NOT contain real job postings (e.g., if it is a directory of companies, a list of categories, or just a generic article),
// you MUST leave the result array empty. Do NOT extract companies as jobs."
//
// Therefore, the 'result' array will remain empty as initialized in the VM context.