$('[data-job-id], .job-listing, .job-item, .career-opening').each(function() {
    // No job listing containers found in the provided HTML sample.
    // The HTML sample provided only contains the <head> section and meta information for a "Careers" page,
    // but not the actual body content with job listings.
    // According to instructions, if the HTML does NOT contain real job postings,
    // the result array MUST be left empty.
    // Therefore, no extraction is performed for this specific HTML input.
});