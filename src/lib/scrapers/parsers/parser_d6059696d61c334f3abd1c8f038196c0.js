// The provided HTML sample only contains the <head> section and does not list actual job postings (title, description, location, etc.).
// It is a page *about* vacancies for "World Vision Tanzania Vacancies 2026", but does not contain the individual job listings themselves.
// According to instruction #2: "If this HTML does NOT contain real job postings (e.g., if it is a directory of companies, a list of categories, or just a generic article), you MUST leave the result array empty. Do NOT extract companies as jobs."
// Therefore, the result array will remain empty.